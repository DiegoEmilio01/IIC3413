{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Test Example",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/Debug/tests/test_example",
            "args": [
            ],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Debug Build"
        }
    ]
}
